# قواعد ودستور المشروع / Project Guidelines & Scope

هذا المستند يعتبر المرجع الأساسي والدائم لتوجيه مهام التطوير البرمجي في هذا المشروع. يجب على أي نموذج ذكاء اصطناعي (Agent) قراءة هذا الملف والالتزام به بشكل كامل لتفادي انحراف مسار المشروع أو تكرار التعديلات المتعارضة.

This document serves as the permanent reference for all development tasks in this workspace. Any AI agent must read and adhere to these guidelines to ensure consistency and avoid scope drift.

---

## 1. الهدف المعماري الأساسي (Core Architecture Goal)
* **المزامنة السحابية هي الأساس (Cloud Sync is Primary):**
  الهدف الأساسي للنظام هو ربط تطبيق الأندرويد مع سحابة **Supabase** (الخطة المجانية - Free Tier) ليكون نظاماً متعدد المستأجرين (Multi-Tenant).
* **معرّف المدرسة وعزل البيانات (School ID & Isolation):**
  كل مدرسة تمثل مستأجراً (Tenant) مستقلاً يُعرف بـ `school_id`. يتم عزل البيانات على مستوى قاعدة البيانات باستخدام سياسات الأمان الخاصة بـ Supabase (Row Level Security - RLS) استناداً إلى رأس الطلب (`x-school-id` Header).
* **الاتصال المحلي كخيار احتياطي فقط (Local Sync is only a Fallback):**
  الاتصال المحلي المباشر بين تطبيق سطح المكتب والجوال عبر الشبكة المحلية (عبر Express Server على منفذ `3000`) هو خيار احتياطي فقط (Fallback/Offline mode). يجب ألا يستبدل أو يلغي البنية التحتية السحابية لـ Supabase.

---

## 2. جدول قاعدة البيانات السحابية (Database Schema)
يتم الربط بشكل مباشر مع جداول Supabase التالية المحددة في ملف [schema.sql](file:///c:/boss/schema.sql):
1. **`schools`**: تحتوي على رمز المدرسة وكود الاقتران.
2. **`teachers`**: بيانات المعلمين والمعرف الخاص بكل معلم.
3. **`classes` & `subjects` & `teacher_assignments`**: الفصول والمواد والمهام الموكلة لكل معلم.
4. **`students`**: سجل الطلاب الخاص بكل مدرسة.
5. **`grades`**: درجات الطلاب وتُخزن بتنسيق JSONB متوافق مع DTO الخاص بتطبيق الأندرويد.
6. **`attendance`**: غيابات الطلاب.

---

## 3. توجيهات هامة للمطور / Developer Guidelines
* **عدم تغيير Credentials افتراضياً:**
  يجب قراءة بيانات الربط (Supabase URL, API Key) ديناميكياً من جدول الإعدادات (`SchoolConfig`) في تطبيق الأندرويد أو من الـ `localStorage` في تطبيق سطح المكتب. لا تقم بكتابة قيم افتراضية ثابتة أو مسح الإعدادات الحالية.
* **سياسات RLS:**
  عند تنفيذ أي استعلام في تطبيق الأندرويد أو سطح المكتب، يجب تمرير رأس الطلب `x-school-id` لضمان أن Supabase يسمح بالوصول للبيانات وتفادي أخطاء الصلاحيات.
* **الربط التلقائي عبر QR:**
  رمز الـ QR الذي ينشئه تطبيق سطح المكتب يجب أن يحتوي دائماً على رابط السحابة `url` الخاص بـ Supabase ومفتاح الوصول `apiKey` الخاص بالمشروع، لتمكين تطبيق الأندرويد من الربط المباشر بالسحابة فور مسح الرمز.

---

## 4. ميثاق وضوابط توليد الجدول المدرسي (Schedule Generation Rules & Charter)
يجب الالتزام الدائم بالموازين التالية دون تعديل أو تخفيف:
1. **استبعاد الدرس السادس:** تُمنع المواد العلمية (الفيزياء، الكيمياء، الأحياء) والمواد ذات النصاب الفردي أو الثنائي ($\le 2$ حصص أسبوعياً كالأخلاقية والدروس الثنائية) من وضعها في الدرس السادس.
2. **سادس الخميس شاغر حتماً:** لا يوضع أي درس حقيقي في الدرس السادس يوم الخميس؛ فهو مخصص للنشاط الحر أو نهاية الدوام لكافة الصفوف.
3. **حصر الشواغر في نهاية الدوام:** أي حصة شاغرة ناتجة عن نصاب يقل عن 30 حصة أسبوعياً توضع حتماً في الدرس السادس بدءاً من يوم الخميس ثم الأربعاء.. إلخ، ويمنع وضع أي شاغر في الدروس (1 إلى 5).
4. **التكافؤ والتوازن العادل في الدرس السادس:** تدوير الحصص المسموحة في الدرس السادس بالتساوي بين المواد والأساتذة ومنع تكرار أي مدرس أو درس أكثر من غيره داخل الشعبة أو على مستوى كادر المدرسة.
5. **منع التكرار اليومي:** عدم تكرار نفس المادة لنفس الشعبة في اليوم الواحد إلا إذا كان نصابها يفوق 5 حصص أسبوعياً.
6. **التوليد المباشر في أيقونة الجدول:** توفير زر توليد آلي مباشر وحفظ فوري في واجهة جدول الحصص يعتمد هذه القواعد ويحدث أنصبة الكادر والسحابة فوراً.
